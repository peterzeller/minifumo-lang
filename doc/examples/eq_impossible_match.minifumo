// Demonstrates an impossible equality match: Eq(True, False) has no constructors.
fun bool_distinct1(h: Eq(True, False)): FalseProp
    // impossible case: true cannot be equal to false
    match h

// Keeps a trailing declaration so the parser observes newline tokens after the empty match.
fun bool_distinct1_parses(): Unit
    ()
