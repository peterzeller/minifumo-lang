export data SizedList[T: Type, N: Nat] =
   SizedNil: SizedList[T, Zero]
 | SizedCons(head: T, tail: SizedList[T, N]): SizedList[T, Suc(N)]

export fun appendSizedList[T: Type, N: Nat, M: Nat](xs: SizedList[T, N], ys: SizedList[T, M]): SizedList[T, natAdd(N, M)]
  match xs
    case SizedNil
      ys
    case SizedCons(h, t)
      SizedCons(h, appendSizedList(t, ys))

/*
export fun sizedListToString[T: Type, PeterN: Nat](s: Show[T], list: SizedList[T, PeterN]): String
    match list
        case SizedNil
            ""
        case SizedCons(head, tail)
            stringAppend(s.show(head), stringAppend(", ", sizedListToString(s, tail)))

fun main(): Unit
  let as = SizedCons[Int][Suc(Zero)](1, SizedCons[Int][Zero](2, SizedNil[Int]))
  let bs = SizedCons[Int][Zero](3, SizedNil[Int])
  let cs = appendSizedList[Int][Suc(Suc(Zero))][Suc(Zero)](as, bs)
  printlnString(sizedListToString(showInt, cs))

// Output:
// 1, 2, 3, 
*/
